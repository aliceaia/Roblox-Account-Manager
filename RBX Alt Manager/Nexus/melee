settings().Rendering.QualityLevel = 1
local a = UserSettings():GetService "UserGameSettings"
a.MasterVolume = 0
CargoShipper = Instance.new("ScreenGui")
Title = Instance.new("TextLabel")
CargoShipper.Name = "CargoShipper"
CargoShipper.Parent = game.CoreGui
CargoShipper.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
Title.Name = "Title"
Title.Parent = CargoShipper
Title.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Title.BackgroundTransparency = 100.000
Title.BorderSizePixel = 0
Title.Size = UDim2.new(0, 189, 0, 52)
Title.Position = UDim2.new(0.225, 0, 0.922, 0)
Title.FontFace = Font.new("rbxasset://fonts/families/PressStart2P.json", Enum.FontWeight.Bold)
Title.Text = "RAM LOADED"
Title.TextColor3 = Color3.fromRGB(217, 88, 28)
Title.TextSize = 14.000
Title.TextWrapped = true
local b = game:GetService("Players").LocalPlayer.Data.Level.Value
local c = game:GetService("Players").LocalPlayer.Name
local d = "http://localhost:7963/SetDescription?Account="
local e = "http://localhost:7963/SetAlias?Account="
local f
if game.PlaceId == 2753915549 then
    f = 1
elseif game.PlaceId == 4442272183 then
    f = 2
elseif game.PlaceId == 7449423635 then
    f = 3
end
syn.request({Url = e .. c, Method = "POST", Body = tostring(f)})
local g = true
local h = game:GetService("UserInputService")
local i = game:GetService "UserInputService"
local j = game:GetService "RunService"
j:Set3dRenderingEnabled(false)
h.InputBegan:Connect(
    function(k)
        if k.KeyCode == Enum.KeyCode.L then
            if g == false then
                j:Set3dRenderingEnabled(false)
                g = true
            else
                j:Set3dRenderingEnabled(true)
                g = false
            end
        end
    end
)
local l
local m = 0
local n = false
local o = false
local p = false
local q = false
local r = false
local s = false
local t
local u
while true do
    if b < 2450 then
        if game:GetService("Players").LocalPlayer.Data.Level.Value > b then
            b = game:GetService("Players").LocalPlayer.Data.Level.Value
            if game.PlaceId == 2753915549 then
                u = 1
            elseif game.PlaceId == 4442272183 then
                u = 2
            elseif game.PlaceId == 7449423635 then
                u = 3
            end
            if u ~= f then
                f = u
                t = syn.request({Url = e .. c, Method = "POST", Body = tostring(f)})
                repeat
                    task.wait()
                until t.Success == true
            end
            t =
                syn.request(
                {
                    Url = d .. c,
                    Method = "POST",
                    Body = tostring(b) .. " " .. tostring(m) .. " " .. os.date("%m/%d/%Y %I:%M:%S %p")
                }
            )
            repeat
                task.wait()
            until t.Success == true
        end
    else
        if n == false then
            local v = {[1] = "BuyDeathStep", [2] = true}
            DeathStep = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if DeathStep == 1 then
                n = true
                m = m + 1
            end
            wait(0.2)
        end
        if o == false then
            local v = {[1] = "BuySuperhuman", [2] = true}
            Superhuman = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if Superhuman == 1 then
                o = true
                m = m + 1
            end
            wait(0.2)
        end
        if p == false then
            local v = {[1] = "BuySharkmanKarate", [2] = true}
            SharkmanKarate = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if SharkmanKarate == 1 then
                p = true
                m = m + 1
            end
            wait(0.2)
        end
        if q == false then
            local v = {[1] = "BuyElectricClaw", [2] = true}
            ElectricClaw = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if ElectricClaw == 1 then
                q = true
                m = m + 1
            end
            wait(0.2)
        end
        if r == false then
            local v = {[1] = "BuyDragonTalon", [2] = true}
            DragonTalon = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if type(DragonTalon) == "number" then
                if DragonTalon == 1 then
                    r = true
                    m = m + 1
                end
            end
            wait(0.2)
        end
        if s == false then
            local v = {[1] = "BuyGodhuman", [2] = true}
            Godhuman = game:GetService("ReplicatedStorage").Remotes.CommF_:InvokeServer(unpack(v))
            if type(Godhuman) == "number" then
                if Godhuman == 1 then
                    s = true
                    l = "Godhuman"
                end
            end
        end
        if m > 2 and l ~= "Godhuman" then
            t =
                syn.request(
                {
                    Url = d .. c,
                    Method = "POST",
                    Body = tostring(b) .. " " .. tostring(m) .. " " .. os.date("%m/%d/%Y %I:%M:%S %p")
                }
            )
            repeat
                task.wait()
            until t.Success == true
            game:Shutdown()
        elseif l == "Godhuman" then
            t =
                syn.request(
                {
                    Url = d .. c,
                    Method = "POST",
                    Body = tostring(b) .. " " .. "GOD" .. " " .. os.date("%m/%d/%Y %I:%M:%S %p")
                }
            )
            repeat
                task.wait()
            until t.Success == true
            game:Shutdown()
        else
            t =
                syn.request(
                {
                    Url = d .. c,
                    Method = "POST",
                    Body = tostring(b) .. " " .. tostring(m) .. " " .. os.date("%m/%d/%Y %I:%M:%S %p")
                }
            )
            repeat
                task.wait()
            until t.Success == true
            wait(60)
        end
    end
end
